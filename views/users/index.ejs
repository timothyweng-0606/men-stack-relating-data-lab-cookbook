<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>List of Users</title>
</head>
<body>
    <a href="/">Back</a>
    <h1>List of users:</h1>
    <ul>
        <% users.forEach(user => { %>
            <a href="/users/show/<%= user._id %>"><li><%= user.username %></li></a>
        <% }); %>
    </ul>
</body>
</html>