<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Detail</title>
</head>
<body>
    <a href="/users">Back</a>
    <h1><%= user.username %>'s Pantry</h1>
  <ul>
    <% user.pantrys.forEach(item => { %>
      <li><%= item.name %></li>
    <% }); %>
  </ul>
</body>
</html>